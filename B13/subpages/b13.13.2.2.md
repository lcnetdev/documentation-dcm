There are two categories of imported AACR2 records that should be converted to RDA:

1. less than full AACR2 records (encoding levels K, M, 3, 5, and 7);
2. records in which the language of cataloging (040 $b) is other than English;
convert to RDA regardless of encoding level.
